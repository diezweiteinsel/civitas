# Database settings used by src/backend/core/db.py
DB_HOST=db
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=postgres

# Optional: enable sqlite dev mode (set to 1 to use in-memory sqlite)
# DEV_SQLITE=1

# Optional: show SQLAlchemy SQL statements
# ECHO_SQL=1
