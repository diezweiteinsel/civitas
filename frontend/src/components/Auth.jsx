import React, { useState, useEffect, createContext} from "react";

const handleSubmit = () => {

    // handles user data upon registration 

}

const authProvider = () => {  // provies access token to web client 

   // const {token, setToken} =  ? 

}